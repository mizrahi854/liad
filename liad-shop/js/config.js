/* ==========================================================================
   LIAD — חיבור למסד הנתונים

   ★ זה הקובץ היחיד שצריך לערוך כדי לחבר את האתר ל-Supabase. ★

   איפה מוצאים את שני הערכים:
     Supabase → הפרויקט שלך → Project Settings (גלגל השיניים) → API

       Project URL  →  SUPABASE_URL
       anon public  →  SUPABASE_ANON_KEY

   שני הערכים האלה נועדו לשבת בקוד של הדפדפן, וזה בסדר גמור שהם
   גלויים — מי שמגן על הנתונים הוא ה-RLS שהגדרנו ב-supabase/schema.sql,
   לא הסתרת המפתח.

   ⚠ אל תשים כאן לעולם את המפתח service_role ואת סיסמת מסד הנתונים.
     הם עוקפים את כל ההגנות, ומי שיפתח את קוד המקור יקבל שליטה מלאה.

   כל עוד הערכים ריקים — האתר עובד בדיוק כמו קודם, עם שמירה מקומית
   בדפדפן בלבד. שום דבר לא נשבר.
   ========================================================================== */

export const SUPABASE_URL = "";
export const SUPABASE_ANON_KEY = "";

/** האם החיבור מוגדר בכלל */
export const hasBackend = () =>
  Boolean(SUPABASE_URL && SUPABASE_ANON_KEY);

/**
 * כל כמה זמן לבדוק אם מישהו שינה משהו במכשיר אחר, במילישניות.
 * רק כשהטאב פתוח ופעיל.
 */
export const SYNC_INTERVAL = 20_000;
